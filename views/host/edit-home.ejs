<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/output.css">
  <title>Edit Home</title>
</head>

<body class="bg-gray-50 font-sans text-gray-900">
  <%- include('../partials/header',{isLoggedIn}) %>

    <main class="max-w-3xl mx-auto px-4 sm:px-6 lg:px-8 py-12">
      <div class="bg-white shadow rounded-lg overflow-hidden">
        <div class="px-6 py-8 border-b border-gray-200">
          <h2 class="text-2xl font-bold text-gray-900">Add a new home</h2>
          <p class="mt-1 text-sm text-gray-500">Fill in the details to list your property.</p>
        </div>
        <form action="/edit-home" method="POST" class="px-6 py-8 space-y-6">
          <div>
            <label for="houseName" class="block text-sm font-medium text-gray-700">House Name</label>
            <div class="mt-1">
              <input type="text" name="houseName" id="houseName" required value="<%=home.houseName%>"
                class="shadow-sm focus:ring-red-500 focus:border-red-500 block w-full sm:text-sm border-gray-300 rounded-md p-2 border"
                placeholder="e.g. Cozy Cottage in the Woods">
            </div>
          </div>

          <div>
            <label for="price" class="block text-sm font-medium text-gray-700">Price per Night ($)</label>
            <div class="mt-1">
              <input type="number" name="price" id="price" required value="<%=home.price%>"
                class="shadow-sm focus:ring-red-500 focus:border-red-500 block w-full sm:text-sm border-gray-300 rounded-md p-2 border"
                placeholder="100">
            </div>
          </div>

          <div>
            <label for="location" class="block text-sm font-medium text-gray-700">Location</label>
            <div class="mt-1">
              <input type="text" name="location" id="location" required value="<%=home.location%>"
                class="shadow-sm focus:ring-red-500 focus:border-red-500 block w-full sm:text-sm border-gray-300 rounded-md p-2 border"
                placeholder="e.g. Malibu, CA">
            </div>
          </div>

          <div>
            <label for="rating" class="block text-sm font-medium text-gray-700">Rating</label>
            <div class="mt-1">
              <input type="number" step="0.1" max="5" name="rating" id="rating" value="<%=home.rating%>"
                class="shadow-sm focus:ring-red-500 focus:border-red-500 block w-full sm:text-sm border-gray-300 rounded-md p-2 border"
                placeholder="4.5">
            </div>
          </div>

          <div>
            <label for="photoUrl" class="block text-sm font-medium text-gray-700">Photos</label>
            <div class="mt-1 cursor-pointer">
              <!-- <input type="file" name="photo" multiple accept="image/png image/jpg image/jpeg" required> -->
              <input type="text" name="photoUrl" id="photoUrl" value="<%=home.photoUrl%>"
                class="shadow-sm focus:ring-red-500 focus:border-red-500 block w-full sm:text-sm border-gray-300 rounded-md p-2 border"
                placeholder="https://example.com/image.jpg">
            </div>
            <input type="text" name="id" value="<%=home._id%>" class="hidden">
          </div>

          <div>
            <label for="description" class="block text-sm font-medium text-gray-700">Description</label>
            <div class="mt-1">
              <textarea name="description" id="description"
                class="shadow-sm focus:ring-red-500 focus:border-red-500 block w-full sm:text-sm border-gray-300 rounded-md p-2 border"
                placeholder="Description"><%=home.description%></textarea>
            </div>
          </div>

          <div class="pt-5 flex justify-end">
            <button type="submit"
              class="ml-3 inline-flex justify-center py-2 px-4 border border-transparent shadow-sm text-sm font-medium rounded-md text-white bg-red-500 hover:bg-red-600 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-red-500">
              Edit House
            </button>
          </div>
        </form>
      </div>
    </main>
</body>

</html>